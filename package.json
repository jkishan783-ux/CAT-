{
  "name": "cat-prep-root",
  "version": "1.0.0",
  "description": "CAT Preparation Portal MERN Root Workspace",
  "scripts": {
    "dev": "npx.cmd concurrently \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\""
  }
}
